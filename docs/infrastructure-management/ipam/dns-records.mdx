---
title: "DNS Records"
sidebar_position: 8
---

import ThemedImage from '@theme/ThemedImage'
import useBaseUrl from '@docusaurus/useBaseUrl'

Device42 supports A/AAAA, CNAME, MX, and PTR records. Records can be added manually or populated automatically using [DNS discovery](auto-discovery/setup-dns-autodiscovery.mdx).

A/AAAA records are automatically associated with existing IP addresses, or can be associated with new IP addresses when they are created.

Navigate to **Resources > DNS > All DNS Records** to view, search, and filter records by domain.

The **Zone Type** column identifies whether a DNS record belongs to a Public or [Private Route 53 hosted zone](/infrastructure-management/ipam/dns-zones). This makes records from split-horizon domains distinguishable even when their domain and nameserver values match.

<ThemedImage
  alt="DNS Records list showing the Zone Type column"
  sources={{
    light: useBaseUrl('/assets/images/dns-records/dns-record-zone-type-light.png'),
    dark: useBaseUrl('/assets/images/dns-records/dns-record-zone-type-dark.png'),
  }}
/>
